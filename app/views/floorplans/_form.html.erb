<%= simple_form_for(@floorplan) do |f| %>
  <%= f.error_notification %>

    <%= f.input :layout_id, label: 'ID' %>
    <% @window_symbols.each do |window| %>
      <%= f.input window, collection: 1..5 %>
    <% end %>

    <%= f.button :submit, class: 'btn btn-block btn-success' %>
<% end %>
