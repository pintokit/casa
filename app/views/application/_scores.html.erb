<% title 'Value Scores' %>
<% heading 'Value Scores' %>

<table class='table table-hover'>
  <thead>
    <tr>
      <th>Flat</th>
      <th>Price per ft<sup>2</sup></th>
      <th>Score</th>
      <th>💲</th>
      <th>🖼</th>
      <th>🌇</th>
      <th>🏢</th>
      <th>📐</th>
    </tr>
  </thead>

  <tbody>
    <% @flats.each do |flat| %>
      <tr>
        <td class="<% if !flat.is_active %>inactive<% end %>">
          <%= link_to flat_path(flat), class: 'btn btn-default' do %>
            <%= flat.name %>
          <% end %>
        </td>
        <td>
          <%= price_per_sqft_link(flat) %>
        </td>
        <td>
          <%= score_column(flat.value_score, 100, 650)%>
        </td>
        <td>
          <%= score_column(flat.price_score, 350, 350)%>
        </td>
        <td>
          <%= score_column(flat.window_score, 100, 100)%>
        </td>
        <td>
          <%= score_column(flat.view_score, 50, 50)%>
        </td>
        <td>
          <%= score_column(flat.floor_score, 50, 50)%>
        </td>
        <td>
          <%= score_column(flat.sqft_score, 100, 100)%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
