<h1>Editing Flat</h1>

<%= render 'form' %>

<%= link_to 'Back to Flats', flats_path, class:'btn btn-block btn-default' %>

<%= link_to @flat, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-block btn-danger' do %>
  <span class='glyphicon glyphicon-trash'></span>
<% end %>
